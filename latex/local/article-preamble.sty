% all related to fonts and encoding
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\inputencoding{utf8}
\usepackage{lmodern}

% everything for the reference list
\usepackage[backend=biber,%
            style=authoryear,%
            doi=false,%
            isbn=false,% 
            url=false,%
            maxcitenames=3,%
            sorting=nyt,%ydnt,nyt, none%
            maxbibnames=99,%
            url=false,%
            eprint=false,%
	    	useprefix=true,%
	    	uniquename=false]{biblatex}	    
\renewcommand*{\nameyeardelim}{\space}
\renewcommand*{\postnotedelim}{\addcolon\space}
\IfFileExists{parts/bibliography.bib}{
	\addbibresource{parts/bibliography.bib}
}{
	\IfFileExists{/home/bob/.bibmanager/bm_bibliography.bib}{
		\addbibresource{/home/bob/.bibmanager/bm_bibliography.bib}
	}{
        \typeout{No bibresource found "|}
        \typeout{try `wget --output-document=parts/bibliography.bib - https://raw.githubusercontent.com/BobBorges/refs-repo/master/bm_bibliography.bib`}
        %\addbibresource[location=remote]{https://raw.githubusercontent.com/BobBorges/refs-repo/master/bm_bibliography.bib}
	}
}

\makeatletter
\AtBeginDocument{\toggletrue{blx@useprefix}}
\AtBeginBibliography{\togglefalse{blx@useprefix}}
\makeatother

%%%%%%%%%%%%%%%
%% FOOTNOTES %%
%%%%%%%%%%%%%%%

% allows easy footnotes formatting

\usepackage[bottom, hang,flushmargin]{footmisc}

% allows \affil in footnotes
\usepackage{authblk}


\usepackage{xcolor, comment}
\newcommand{\rb}[1]{\textcolor{blue}{[Bob: #1]}}
\newcommand{\anon}[1]{\textcolor{green}{[Anonymous comment: #1]}}
\newcommand{\nobib}[1]{\textcolor{red}{[No bibtex for: #1]}}


% allows hyperlinks
% MUST BE LAST
\usepackage[hidelinks, hyperfootnotes=false]{hyperref}

